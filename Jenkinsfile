pipeline {
    agent any
    stages {
       stage('build') {
           steps {
	       echo'build has been comlated'
	       sh 'echo Integrating pipeline using Jenkinsfile'
	       	}
	     }
    }
}