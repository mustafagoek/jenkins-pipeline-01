pipeline {
    agent any
    stages {
       stage('build') {
           steps {
	       echo'build has been started'
	       sh 'echo Integrating pipeline using Jenkinsfile'
	       	}
	     }
    }
}