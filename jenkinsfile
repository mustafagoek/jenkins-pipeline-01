pipeline {
    agent any
    stages {
       stage('build') {
           steps {
	       sh 'echo build has been started'
	       sh ' echo Integrating pipeline using Jenkinsfile and webhook'
	       sh 'ls'
	       }
	     }
    }
}